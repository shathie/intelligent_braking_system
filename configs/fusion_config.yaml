model:
  name: "cross_modal_attention"
  vit_dim: 512
  temporal_dim: 256
  fusion_dim: 512
  num_heads: 8
  num_layers: 2
  dropout: 0.1

training:
  batch_size: 8
  learning_rate: 1e-4
  weight_decay: 0.01
  epochs: 150